import sys
import json
import re


#script

def check_password_strength(password):









	return strength # as string, create list of strengths?

if __name__ == "__main__":
	password = sys.argv[1]
	print(json.dumps({"strength": check_password_strength(password)}))
